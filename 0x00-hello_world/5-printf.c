#include <stdio.h>
/**
 *main - print the string in thr printf function
 *Description: main function that prints to the stdout.
 *This program prints "with proper grammar,
 *but the outcome is a piece of art,"
 *Return: 0if success.
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
