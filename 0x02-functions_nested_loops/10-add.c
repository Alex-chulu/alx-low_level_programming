#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: first int used in the addition
 * @b: second int used in the addition
 * Return: Always 0
 */
int add(int a, int b)
{
	return (a + b);
}
