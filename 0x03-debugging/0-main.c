#include "main.h"
/**
 * 0-main - It test that the function positive_or_negative()
 * gives the correct output when given a case of 0
 * Return: Always 0 (success)
 */
int main()
{
	int i;

	i = 0;
	positive_or_negative();

	return (0);
}
